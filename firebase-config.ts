
/**
 * Firebase Project Configuration
 * Replace the values below with your actual Firebase project settings.
 * You can find these in the Firebase Console: Project Settings > General > Your apps.
 */
export const FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

/**
 * VAPID KEY
 * Place your "Web Push certificates" key pair here.
 * Find it in: Firebase Console > Project Settings > Cloud Messaging > Web configuration.
 */
export const VAPID_KEY = "YOUR_PUBLIC_VAPID_KEY_HERE";
